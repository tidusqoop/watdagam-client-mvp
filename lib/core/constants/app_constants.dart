/// Application-wide constants
class AppConstants {
  static const String APP_NAME = '왔다감';
  static const Duration DRAG_UPDATE_DELAY = Duration(milliseconds: 200);
  static const Duration ZOOM_INDICATOR_DURATION = Duration(milliseconds: 2000);
}